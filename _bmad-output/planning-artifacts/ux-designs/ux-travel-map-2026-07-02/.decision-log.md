# Decision Log — UX: Mapsake v2 (native iOS)

Canonical memory for this UX run. Spines are distilled from here at Finalize; spines win on conflict.

## 2026-07-08 — Visual direction (Simon, brain dump)

- **Visual identity CARRIES OVER from v1:** Simon likes v1's visual tone — the color palette and typography stay, adapted to mobile/iOS. DESIGN.md = v1 tokens inherited + iOS-native adaptation deltas (no re-imagining).
- **The uplift is EXPERIENCE, not look:** "it is not so easy to add memory or pin to a location now, and photo too" — capture friction is the pain; the PRD's new flows (search-first, long-press, date-suggested photos, loop + recap) are the answer. UX-run effort concentrates on interaction design, flow choreography, and the capture ritual's feel.
- **Gripe (new): the v1 PWA login zooms the viewport** (iOS Safari auto-zoom on <16px inputs). v2 anti-pattern logged: **no unexpected scale/viewport jumps, ever** — native SwiftUI removes the mechanism, but the principle guards all focus/transition moments. (Also a candidate one-line fix for the v1 web app: input font-size ≥16px — noted for deferred-work.)
- **Voice anchor: LINE (TW)** — Simon's named reference for zh-TW copy done right: friendly, concise, everyday-Taiwan register. Calibrates the 語感 guide alongside his approved 「開啟通知，重溫你的旅行」.
- **Imports:** none for now. Gripes sweep: still open (one added today; Simon hasn't declared the list complete).

## 2026-07-08 — 語感 voice guide LOCKED (FR28 gate open)

- Simon arbitrated all four open vocabulary calls: capture verb = **記下** (rejected 封存 as too ceremonial); visit = **第 N 次** pure counter (rejected 旅程/造訪); save success = **已儲存 everywhere** (rejected 已封存 special-casing); recap = **「你記錄了…」** factual-warm pattern (his own line; rejected 都收好了).
- **Master calibration captured:** poetry belongs to positioning (App Store / brand), the UI speaks plain — Simon declined the poetic option in all four picks. The save ANIMATION carries the ritual emotion; the copy stays light. Guide at `.working/voice-guide.md`; all future screen copy drafts against it, Simon blesses before implementation.
- Screens may now be designed. Next: capture-ritual key screens (the pain point), then Claude Design sync + phone prototype.

## 2026-07-08 — Capture-flow prototype round 1 (built, reviewed by Simon, revised)

- Built `.working/capture-flow-prototype.html` — 7 tap-through screens (~390pt frame, v1 tokens, voice-guide copy): home map (region-count clusters, search pill, tab bar with 地圖/去過的地方/設定), search (names+addresses, 「你去過 N 次」 hint), fine-tune pin + address, date chips, date-suggested photo grid, save moment (settle+ripple animation, Reduce-Motion-guarded), loop choice, session recap. Served at localhost:8765 + LAN for Simon's iPhone.
- **Simon's round-1 corrections (all applied):** recap rows drop 第 N 次 (clutter); search-pill icon larger; 反查地址 jargon removed; fine-tune button → **選擇地點**; date heading → **「哪一天去的呢？」**; 先不填 chip removed (略過 suffices); photo step → **上傳** verbs (下一步：上傳照片 / 上傳（N 張照片）); 建議而已 caption replaced with a **＋其他照片 picker tile** (function over reassurance); loop button → **記錄下個地方**.
- **Voice lesson (guide updated):** the capture verb settles as **記錄** (superseding 記下 from the earlier arbitration — Simon's own button rewrites chose it); buttons name concrete actions; no reassurance-copy where a control belongs; one skip affordance per screen; internal jargon never surfaces.

## 2026-07-08 — Round 2: date chips simplified + 去過的地方 + 設定 screens built

- **Date screen (Simon):** 選其他日期 chip removed — the wheel below IS the other-date path. Chips = true shortcuts only: **「今天」 + the previous place's date in this session** (「5 月 17 日 · 上個地點」 — logging a trip means consecutive places share dates; the session is the suggestion source).
- **去過的地方 (#places) built:** geographic hierarchy per FR20 — continent section headers (亞洲/歐洲) → country rows (flag, 「12 個回憶 · 3 個地區」) → expanded country shows region rows with place-name previews (「清水寺 · 金閣寺 · 嵐山竹林 +4」) → tap dives to the map. In-collection search field on top (FR21). First-class tab per FR19.
- **設定 (#settings) built (FR26/FR30):** profile card (avatar, email, sign-in method); grouped iOS-style sections — 通知 (回憶通知 toggle + 每天傍晚 7:00, 靜音的地方), 地圖 (預設畫面：整個世界), 資料 (匯出我的回憶, 刪除帳號 in warning red), quiet 登出. The account surface is IN the settings screen as its design home.
- Tab bar now fully wired across home/places/settings.
- **地方 → 地點 app-wide (Simon):** one place-noun everywhere (去過的地點, 靜音的地點, 記錄下個地點, 你記錄了 N 個地點) — consistent with 選擇地點. Also swept two 記下 stragglers (search pill + long-press hint) to 記錄. Voice guide updated; v2 diverges from v1's 地方 deliberately.

## 2026-07-08 — Round 3: the soul screens + first-run (frames 10-14)

- **#notif — lock-screen rich notification:** dark lock screen, Mapsake card with app icon + 「2 年前的今天：清水寺」 + 「點開重溫這段回憶」 + the memory's photo thumbnail (the FR14 tap-earner). Tapping it lands on #relive.
- **#relive — the landing (UJ-2/FR15):** map flies to the pin with a soft pulsing glow (Reduce-Motion → static); bottom sheet: place name, terracotta context line 「2 年前的今天 · 2024 年 7 月 9 日 · 第 2 次」, THAT visit's photo strip + note, 「這天還有 2 個回憶 →」 cohort chip (FR16), quiet ⋯ overflow (mute lives there; graduated curation still an open question).
- **#intro — first-run brand moment:** pin mark, Mapsake, 「你的私人旅行時光膠囊」 (the ONE surface where positioning poetry is allowed per the voice guide), two-line promise, page dots, 開始.
- **#signin (FR23):** Apple (black, first) / Google / Email buttons, reassurance line 「你的地圖會安全地保存，換手機也不會不見。」, returning-web-user steering 「用過網頁版的 Mapsake？用同一種方式登入就好。」. Sign-in → straight into #search: **the guided first memory IS the capture flow** (UJ-4 = UJ-1 mini, no separate onboarding capture UI).
- **#notifask (FR24):** post-first-memory pre-prompt — a mini preview of the future notification (their own place in it), Simon's approved 「開啟通知，重溫你的旅行」, sub 「明年的今天，Mapsake 會把這段回憶帶回來給你。」, 開啟通知 / 先不用.
- Prototype now covers ALL IA surfaces: 14 frames. Pending: Simon's pass on frames 10-14, then Claude Design sync.

## 2026-07-08 — Simon approved all 14 frames → synced to Claude Design

- Simon: re-live 「looks good」, frames 10-14 「they are good」. (iOS Safari :target bug fixed with JS hash navigation first — the CSS-only switching didn't fire on iPhone taps.)
- Prototype split into 16 self-contained @dsCard preview cards (script: `.working/build-ds-cards.mjs` → `.working/ds-cards/`): 14 screens (groups: Core / Capture / Re-live / First-run, 390×844 viewports) + 2 Foundations cards (Sepia Parchment palette, Noto Serif/Sans TC type scale).
- **Pushed to the "Mapsake v2" Claude Design project** (projectId 4a68b885-50aa-478e-a6b5-f3c0a20162eb): 16 files written, 16 cards registered. Simon reviews/edits at claude.ai/design; canvas edits fold back into the spines/prototype on his word.
- Remaining to close the UX phase: distill DESIGN.md + EXPERIENCE.md from this log + working artifacts; reviewer gate (opt-in); open-items triage (session-recap trigger, empty-memory granularity, graduated curation — some now have de-facto answers in the approved frames); promote keepers to mockups/; polish + status:final.

## 2026-07-09 — Finalize remediation (all reviewer/reconciler findings resolved)

- **Note entry (Simon): on the photo step** — optional expanding 「想寫點什麼…」 field below the grid; saves per-visit (FR9); one screen serves photos + words. Prototype + EXPERIENCE updated; UJ-1/UJ-4 mention it.
- **Approximate-pin backfill folded in** (was the missed fold-in): EXPERIENCE "Region backfill" pattern (「記錄這個地區」 → region-center pin) + DESIGN `fill-approximate` (parchment fill, dashed terracotta, no dot; solidifies when dragged).
- **Selected-chip AA failure RESOLVED:** text switches terracotta → **ink** on the terracotta-soft tint (8.42:1); spine tokens, body, contrast table, and prototype all updated. Terracotta-on-tint banned for text.
- **Save-animation geometry** (settle keyframes + ripple anatomy) now in DESIGN `motion-save`/`motion-glow` — spines are rebuild-complete without the HTML.
- **17pt input floor enforced:** the 去過的地點 search field frame violated it (15px) — fixed to the `{components.field}` default; floor holds app-wide.
- **Visited-fill ambiguity resolved:** fill = terracotta-hero at 55% over land (decorative); the hatch is the load-bearing non-color cue; token stays full-strength for boundary legibility. Documented in DESIGN Colors.
- **Four missing component specs added:** recap-card, signin-buttons (Apple `#1A1613` = the sanctioned non-token fill), notif-preview-card, browse-expand-card. Type ramp gains intro-tagline (18 serif) + screen-subtitle (14/400).
- **Multi-visit pin-sheet anatomy specified:** opens on most recent visit; visit-history rows below; deep-links open the targeted visit.
- **A11y HIGHs closed in-spine:** fine-tune gains tap-to-move (non-drag equivalent) + VO nudge actions with search as the designated accessible path; per-surface VO reading order + focus management table; 44pt enumeration extended (⋯ at 44pt — prototype fixed; chips padded; toggle row-tappable); keyboard/focus spec (auto-focus, return=search, 2px terracotta focus ring); Dynamic Type per-surface rules (chip wrap, Large Content Viewer, cluster cap, row reflow).
- **Sign-in/search/zero-memory states added** (email wait 「看看你的信箱」, cancel = quiet return; no-results invitation + geocode retry; bare-map/empty-browse invitations). **語言 static row restored** to settings (FR26). Intro two-line promise recorded as approved copy. 清水斷崖 typo fixed. Voice-guide self-contradictions cleaned (rejected 封存-register examples replaced; 地點 sweep in usage examples). Open Items sections added to BOTH spines (owned lists).
- Artifacts promoted: `mockups/capture-flow-prototype.html` + `mockups/voice-guide.md` (all spine references now resolve); server re-rooted at the run dir. Cards rebuilt from the fixed prototype; **re-push to Claude Design pending re-auth** (login token rotated).
- FR12 resolution flowed back to the PRD Assumptions Index (#2 → resolved: explicit button).

## 2026-07-09 — UX PHASE FINALIZED

- Polish pass complete (structure + prose per doc; 3 fixes DESIGN.md, 4 EXPERIENCE.md; guardrails held). Both spines set `status: final`, `updated: 2026-07-09`.
- Refreshed 14 screen cards re-pushed to the "Mapsake v2" Claude Design project (post re-auth). Claude Design is CURRENT with the remediated prototype.
- Artifacts: DESIGN.md + EXPERIENCE.md (final), mockups/ (capture-flow-prototype.html, voice-guide.md), .working/ (ds-cards + build script + screenshots), reconcile-*.md ×3, review-*.md ×2, this log.
- Open items carried forward in both spines' Open Items sections (owners named). Next phase: **bmad-create-architecture** — multi-visit migration (⛔), geocoding provider spike, APNs/NSE design, SwiftUI project structure.

## 2026-07-08 — Open-items triage (during finalize)

- **Backfill granularity (PRD §6, RESOLVED — Simon): approximate pin.** Searching a region/country in capture offers 「記錄這個地區」 → one pin at the region's center, styled APPROXIMATE (hollow/dashed marker variant), region colors visited. One write model preserved (everything is a pin); honest about imprecision; upgradeable by dragging to a real spot later. → EXPERIENCE.md Component Patterns + DESIGN.md pin-marker variant (to be folded in post-distillation since the distiller launched before this decision).
- **Session-recap trigger (PRD FR12, RESOLVED de facto):** the approved prototype uses the explicit 完成這次記錄 button — no inferred session end. FR12's assumption closes toward explicit.
- **Graduated curation (PRD §6, carried forward):** the landing's ⋯ overflow is its home; the graduated options (per-visit "show less") are a post-v2 refinement — v2 ships place-mute there. Carried to architecture/epics as a noted deferral.

## 2026-07-02 — Inherited constraints (from the FINAL v2 PRD — designed here, not re-litigated)

- **Form factor:** iPhone (~390pt), SwiftUI-native; adaptive groundwork for later iPad. UI system = native SwiftUI (spines inherit iOS defaults; specify the delta).
- **Locked product decisions to design:** read-only map + long-press capture; search-first capture (names + addresses) with draggable fine-tune pin + reverse-geocode preview; multi-visit pins (per-visit date/note/photos); date-suggested photo picking (contextual permission, full/limited honored); save moment (animation + haptic, Reduce-Motion-aware); capture loop + session recap; backfill = batch empty memories; signed-in-first (Apple/Google/email) with warm 2-3 screen intro → guided first memory → contextual notification ask; photo-rich re-live notification → lands on THAT DATE's visit; "N more from this day"; geographic browse hierarchy (continent→country→region→place) in first-class navigation; designed settings + account surfaces (FR26); export; one coherent notification-control surface (FR17).
- **Voice:** FR28 — the zh-TW 語感 voice guide comes FIRST, before any screens. Native Taiwan voice, never translation-shaped. Simon is the arbiter. Reference tone: 「開啟通知，重溫你的旅行」 (imperative + benefit, 4+4 rhythm; NOT soft questions). Capture verb concept: lock/seal into the 時光膠囊 (封存-family language candidates; Simon picks final wording).
- **UX-owned open questions (from PRD §6):** session-recap trigger (FR12 — inferred vs explicit end); empty-memory placement granularity (FR11); graduated re-live curation beyond binary mute (FR17); sweep Simon's unfinished v1 design-gripes list.
- **References:** Google Maps (map feel), Apple Photos + Memories notifications (gallery + re-live gold standard). Anti-pattern posture from PRD: no feeds, no streaks, no completion gamification, no scarcity mechanics.
- **Accessibility floor (NFR8):** VoiceOver everywhere, Dynamic Type incl. zh-TW scaling, Reduce Motion honored (save animation included), AA contrast on parchment palette, ≥44pt targets, visited-state never color-alone.
- **Deliverables:** design system + screens as ~390pt preview cards synced to a "Mapsake v2" Claude Design project (Simon reviews/edits there) + a tap-through HTML prototype served to Simon's iPhone; converged screens = the SwiftUI reference.
- **Inheritance:** v1 DESIGN.md/EXPERIENCE.md (parchment/terracotta keepsake identity) — "the visual language evolves, the soul doesn't."
