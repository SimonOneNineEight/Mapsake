# Update these with your Supabase details from your project settings > API
# https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-or-anon-key

# Web Push / VAPID keys (Story 5.1+). Generate with: npx web-push generate-vapid-keys
# The PUBLIC key is exposed to the client (push subscription); the PRIVATE key is SERVER-ONLY
# and is used by the Story 5-3 cron sender to sign pushes — never expose it to the client.
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
# Optional contact the push service can reach (defaults to mailto:hello@mapsake.app).
# VAPID_SUBJECT=mailto:you@example.com

# On-this-day cron sender (Story 5.3) — ALL server-only, never NEXT_PUBLIC_, never commit real values.
# Service-role key (Supabase project settings > API): RLS-bypassing; read by the on-this-day job ONLY.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# Any high-entropy string. Vercel sends it as the cron's `Authorization: Bearer`; the route 401s otherwise.
CRON_SECRET=your-cron-secret
